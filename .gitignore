node_modules/
.sst/
.next/
.open-next/
.turbo/
out/
dist/
*.tsbuildinfo
.DS_Store
test-results/

# Built by `pnpm vision:build-worker` — the scan worker's JS entry point.
# Deliberately a build artefact: worker_threads needs JavaScript, and keeping
# the engine behind one generated file is what keeps onnxruntime-node off
# open-next's dependency trace.
*/.vision/

# Generated by admin-web install route — not part of source
pnpm-workspace.yaml
pnpm-lock.yaml
*/public/starkeep-runtime-config.json
*/infra/photos-cloud-config.json
*/infra/src/web-assets.json

# Environment files
.env
.env.*
!.env.example
