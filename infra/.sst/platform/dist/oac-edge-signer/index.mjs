import { createRequire as topLevelCreateRequire } from 'module';const require = topLevelCreateRequire(import.meta.url);
import a from"node:crypto";var n=async o=>{let e=o.Records[0].cf.request;if(!["POST","PUT","PATCH"].includes(e.method))return e;if(e.body?.inputTruncated)return{status:"413",statusDescription:"Payload Too Large",headers:{"content-type":[{key:"Content-Type",value:"application/json"}]},body:JSON.stringify({error:"Request body exceeds 1MB Lambda@Edge limit. Use presigned S3 URLs for large uploads."})};try{let r=e.body?.data?e.body.encoding==="base64"?Buffer.from(e.body.data,"base64"):Buffer.from(e.body.data):Buffer.alloc(0),t=a.createHash("sha256").update(r).digest("hex");e.headers["x-amz-content-sha256"]=[{key:"x-amz-content-sha256",value:t}]}catch(r){console.error("Error computing SHA256 hash:",r)}return e};export{n as handler};
